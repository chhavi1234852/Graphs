#include <iostream>
using namespace std;
int adj[100][100];
int vis[100];
int V;
void dfs(int u) {
    vis[u] = 1;
    cout << u << " ";
for (int v = 0; v < V; v++) {
        if (adj[u][v] == 1 && vis[v] == 0) {
            dfs(v);
        }
    }
}
int main() {
    int E;
    cin >> V >> E;
 for (int i = 0; i < V; i++)
        for (int j = 0; j < V; j++)
            adj[i][j] = 0;

    for (int i = 0; i < E; i++) {
        int u, v;
        cin >> u >> v;
        adj[u][v] = 1;
        adj[v][u] = 1;
    }
for (int i = 0; i < V; i++)
        vis[i] = 0;
    int start;
    cin >> start;
    dfs(start);
return 0;
}
