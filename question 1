#include <iostream>
using namespace std;
int main() {
    int V, E;
    cin >> V >> E;
int adj[100][100]; 
    for (int i = 0; i < V; i++)
        for (int j = 0; j < V; j++)
            adj[i][j] = 0;
for (int i = 0; i < E; i++) {
        int u, v;
        cin >> u >> v;
        adj[u][v] = 1;
        adj[v][u] = 1;
    }
    int start;
    cin >> start;
    int vis[100] = {0};
    int q[100], front = 0, rear = 0;
    vis[start] = 1;
    q[rear++] = start;
while (front != rear) {
        int u = q[front++];
        cout << u << " ";
for (int v = 0; v < V; v++) {
            if (adj[u][v] == 1 && vis[v] == 0) {
                vis[v] = 1;
                q[rear++] = v;
            }
        }
    }
return 0;
}
